// Web shims for Expo native modules that don't support web properly.
// These modules call registerWebModule() which fails in static export.
// This file provides no-op implementations for web platform.

module.exports = {};
